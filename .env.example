DATABASE_URL=postgres://postgres:postgres@127.0.0.1:5432/trip_planner
# Set in production. Dev falls back to a fixed insecure value.
SESSION_SECRET=change-me
# When unset, magic-link emails are logged to the console instead of sent.
EMAIL_FROM=
